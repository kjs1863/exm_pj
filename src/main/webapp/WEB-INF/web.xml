<?xml version="1.0" encoding="UTF-8"?>
<web-app version="4.0" xmlns="http://xmlns.jcp.org/xml/ns/javaee" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee                       http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd">
	<filter>
		<filter-name>filter</filter-name>
		<filter-class>com.cos.filter.WebFilter</filter-class>
	</filter>
	
	<!-- 해당 필터는 모든 필터들 중에서 가장 마지막에 실행되어야 하는 필터(필터순서 끝번) -->
	<!-- 필터순서는 filter-mapping 여러개 있을 때 위에서 부터 순차적으로 실행됨 -->
	<filter-mapping>
		<filter-name>filter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>
	
	<filter>
		<filter-name>encodingFilter</filter-name>
		<filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
		<init-param>
			<param-name>encoding</param-name>
			<param-value>utf-8</param-value>
		</init-param>
	</filter>
	
	<filter-mapping>
		<filter-name>encodingFilter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>
	
	
	
<!-- 	<servlet>
		<servlet-name>dispatcher</servlet-name>
		<servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
	</servlet> -->
	
	<servlet>
		<servlet-name>dispatcher</servlet-name>
		<servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
		<init-param>
			<param-name>contextConfigLocation</param-name>
			<param-value>/WEB-INF/spring/*-context.xml</param-value>
			<!-- <param-value>/WEB-INF/spring/service-context.xml</param-value> -->
		</init-param>
		<load-on-startup>1</load-on-startup> <!-- 톰켓 실행시 메모리에 올라가기 위함! -->
		<async-supported>true</async-supported> <!-- 비동기 방식으로 처리하기 위함! -->
	</servlet>
	
	<servlet-mapping>
		<servlet-name>dispatcher</servlet-name>
		<url-pattern>/</url-pattern>
	</servlet-mapping>
	
	

</web-app>